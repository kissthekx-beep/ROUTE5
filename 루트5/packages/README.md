# packages

루트5 제품의 재사용 모듈입니다.

- `core`: 감정, 예산, 루트, 루트북의 순수 도메인 규칙
- `catalog`: 장소 데이터 조회와 필터링
- `recommendation`: 루트 조합, 스코어링, 후보 생성
- `platform`: OpenAI, 지도, 장소 API, 저장소 같은 외부 어댑터
- `ui`: 재사용 UI 컴포넌트

의존 방향은 앱에서 패키지로 내려가고, 코어가 앱이나 플랫폼에 직접 의존하지 않도록 유지합니다.
